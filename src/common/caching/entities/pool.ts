export type CachePoolContract = {
  address: string;
  lpTokenId: string;
  tokenIds: string[];
  reserves: string[];
  totalSupply: string;
  swapFeePercent: string;
  adminFeePercent: string;
  ampFactor: string;
  state: boolean;
};
